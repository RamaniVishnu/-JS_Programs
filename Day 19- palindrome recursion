const isPalindrome = str => {
    const strLen = str.length;
    if (strLen < 2) return true;
    if (str[0] === str[strLen - 1]) {
        return isPalindrome( str.slice(1, strLen - 1) );
    }
    return false;
};
console.log(isPalindrome('madam'));



updated code
function palindrome(value){
if(value.length < 3) return true;
return value[0] === value[value.length -1] ? palindrome(value.slice(1,value.length - 1)) : false;
}
console.log(palindrome("deified"));



enhanced this code: 1 without using built in methods
let a="Mom";
function isPalindrome(data){
    if(!data.trim()) return false;
    function check(str){
        if(str.length <= 1) return true;
        return str[0] === str[str.length - 1] && check(str.slice(1,-1))
    }
    return check(data)
};
console.log(isPalindrome(a.toLowerCase())) //true;
console.log(isPalindrome('deified')); // true
console.log(isPalindrome('racecar')); // true
console.log(isPalindrome('hello'));   // false
console.log(isPalindrome(''));        // false


enhanced the code: 2
function isPalindrome(val){
    val=val.trim();
    if(!val) return false;
    return val === val.split('').reverse().join('');    
}
